# FrontWorlds Command — المعمارية

## طبقات التطبيق

تحتوي React على إطار الواجهة ولوحات القيادة والحالة، بينما يدير `client/src/game/` بيانات الدول والمدن والموارد والأوامر بصيغة TypeScript مستقلة عن React. خريطة العرض عبارة عن سطح تفاعلي مسطح مع طبقة Babylon خفيفة لرسم شبكة الإحداثيات ومسارات الحركة.

## الوحدات

- `GameWorld`: الحالة المركزية للدولة المملوكة، الدول الافتراضية، المدن، الموارد، الوحدات، التحالفات، والمؤقتات.
- `gameData`: بيانات مبسطة للدول والمدن والمطارات ومسارات الكباري.
- `GameCanvas`: تهيئة Babylon مرة واحدة، رسم الشبكة والمسارات، وتنظيف الموارد عند الإزالة.
- `Home`: غرفة القيادة؛ تجمع الخريطة، لوحة الدولة، شريط الأوامر، سجل الأحداث، ونوافذ التأكيد.
- `index.css`: نظام الألوان والطباعة والحركة الخاص باتجاه غرفة عمليات الأطلس.

## قرار النسخة الأولى

لا يوجد اتصال شبكي أو حسابات في هذه المرحلة. الخصوم الافتراضيون يتغيرون عبر مؤقتات محلية، والبيانات تحفظ أثناء الجلسة فقط. تصميم النماذج يترك مجالًا لإضافة backend وWebSocket لاحقًا دون إعادة بناء واجهة القيادة.
